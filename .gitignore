node_modules/
*.tsbuildinfo

# Environment
.env
.env.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# ADO config, token cache, and synced work items
.claude/.ado-config.yaml
.claude/.ado-token-cache.json
.claude/ado/work-items/
.claude/ado/.ado-sync/

# Coverage
coverage/

# Note: dist/ is intentionally NOT ignored — dist/cli.js is committed
# for marketplace installs that don't run a build step.
